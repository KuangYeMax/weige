# 需求评估请求：微信好评系统的「网页端 + 朋友端 exe」架构

> 下面是一条完整需求描述，请把整段作为评估输入，给出架构评审与改进建议。

## 一、项目背景
我在做一个「微信好评自动发送系统」（FastAPI 后端 + 前端页面，单机 Windows 运行），给自己和几个朋友用。用途：上传商品图 → 调用方舟 / 百炼的大模型 API 生成「好评图 + 文案」→ 通过微信自动发送给客户。

关键点：
- 方舟、百炼近期有注册送额度活动，朋友们各自用不同手机号注册了账号，每人有自己的 API Key 和额度。
- 我作为系统维护者，用朋友们的 key 帮他们生图（朋友们把 key 填到系统设置里）。
- 微信发送用「坐标点击 + 剪贴板」模拟本机微信客户端操作，**只能在登录了该微信的 Windows 机器上运行**（硬限制：网页 / 服务器都无法远程操控好友列表或替朋友发消息）。

## 二、为什么做成「网页端 + 朋友端 exe」两部分
1. 生图只是对方舟 / 百炼的 HTTP 接口调用，可以集中在我托管的服务器上做；但微信发送必须发生在「登录了对应微信的那台 Windows」上——朋友的客户在朋友自己的微信里，我的服务器够不着。
2. 因此**网页端（我的服务器）= 大脑**：朋友填 key、传产品、我帮生图、排待发队列；**朋友端 exe（朋友自己的 Windows）= 手脚**：轮询服务器拉取「属于该朋友的」待发消息，下载图 + 文案，用朋友本机微信发出去，并回报结果。
3. 纯网页做不到发送（浏览器 / 服务器都无法操控朋友本地微信），所以必须有一个跑在朋友机器上的轻量发送端（打包成 exe，双击即用）。

## 三、朋友身份用 Token（含「只填一次」的体验要求）
目的：区分「这是哪位朋友的 key / 待发队列」，避免全局共用一个 key 互相覆盖；同时不想做账号密码体系（就几个朋友，太重）。

Token 使用方式（重点）：
- **Token 只需填写一次，不应每次都填。** 网页端首次填写 token 后由浏览器本地记住（localStorage / cookie），之后访问免填；朋友端 exe 首次配置 token 后持久化到本地配置文件，之后启动免填。
- 朋友在网页上填一次 token + 自己的 key，后续只管传产品、看进度；exe 装好配一次 token，之后常驻后台自动领任务发送。
- 全局唯一 token 绑定该朋友的 key 与待发队列，服务端据此隔离数据。

## 四、解决方案概述（待评估）
- **Phase 0（地基 + 解决 key 回传）**：把现在明文写进 `.env` 的 API Key，改为加密（Fernet）存入数据库、按朋友隔离的多 key 表；设置页改为「key 管理」（列表 / 新增 / 编辑 / 删除，只显示备注不显明文）。朋友在网页存 key 即等于自动「回传」给我，无需再手动发我，且 key 不进聊天记录、加密落库。
- **Phase 1（按人取 key 生图）**：产品 / 任务绑定 friend_id / key_id；生图时按对应朋友的 key 调方舟 / 百炼。
- **Phase 2（拆分发送 + 朋友端 exe）**：服务器生图后把任务标「待朋友发送」并暴露拉取接口（按 token 返回该朋友的任务 + 图 / 文案下载）；sender.py 抽成可独立运行、可配置服务器地址的 exe 给朋友。
- **Phase 3（可选）**：key 更新时 webhook 通知我（只发「谁更新了 key」信号，不发 key 内容）。

## 五、请评估的维度
1. 架构可行性：网页端 + 朋友端 exe 的拆分是否合理？有无更简方案？
2. 安全性：key 加密落库、token 一次性体验、key 不进聊天记录，是否足够？token 泄露风险与缓解手段？
3. 体验：token「只填一次」在网页与 exe 两侧分别如何稳妥实现（持久化、换机 / 清缓存 / 重装怎么办）？
4. exe 分发与运维：PyInstaller 打包、朋友机器环境依赖、自动更新是否必要？
5. 其他你发现的隐患或更好的做法。

请基于以上给出架构评审与改进建议。
